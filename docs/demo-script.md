# Demo Script

## 目标演示链路

1. 机器人处于 `idle`，自然眨眼。
2. 用户说出唤醒词，切换到 `noticed`。
3. 麦克风收音时播放 `listening`。
4. 等待智能体回复时播放 `thinking`。
5. 智能体根据语义输出情绪，设备显示对应表情并联动动作。
6. 可选加分项：上传宠物照片，生成定制身份头像并显示到机器人。

## 兜底方案

- 云端不可用：用按钮或串口 CLI 触发表情状态。
- AI 生成超时：使用预先生成的宠物身份包。
- 动画解码性能不足：改用 PNG/RGB565 帧序列。

